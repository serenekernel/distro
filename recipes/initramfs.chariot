custom/initramfs {
    dependencies: [ custom/kernel package/hello ]
    options: [ "arch" ]
    build: <sh>
        (cd $SYSROOT_DIR && tar -cf $BUILD_DIR/initramfs.tar . -H ustar)
    </sh>
    install: <sh>install initramfs.tar $INSTALL_DIR</sh>
}
